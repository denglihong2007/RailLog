class StationPairDistance {
  const StationPairDistance({
    required this.fromStation,
    required this.toStation,
    required this.distanceKm,
  });

  final String fromStation;
  final String toStation;
  final double? distanceKm;
}
